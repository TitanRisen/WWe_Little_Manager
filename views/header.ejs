<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8"/>
<title>审核系统</title>
<link rel="stylesheet" href="/stylesheets/style.css">
</head>
<style>
  span {
    font-weight:bold;
    color:#ff9955;
    border: 1px;
 }

button {
  width: 130px;
  height: 40px;
  background: linear-gradient(to bottom, #4eb5e5 0%,#389ed5 100%); /* W3C */
  border: none;
  border-radius: 5px;
  position: relative;
  border-bottom: 4px solid #2b8bc6;
  color: #fbfbfb;
  font-weight: 600;
  font-family: 'Open Sans', sans-serif;
  text-shadow: 1px 1px 1px rgba(0,0,0,.4);
  font-size: 15px;
  text-align: left;
  text-indent: 5px;
  box-shadow: 0px 3px 0px 0px rgba(0,0,0,.2);
  cursor: pointer;

/* Just for presentation */  
  display: block;
  margin: 0 auto;
  margin-bottom: 20px;
}
button:active {
  box-shadow: 0px 2px 0px 0px rgba(0,0,0,.2);
  top: 1px;
}

button:after {
  content: "";
  width: 0;
  height: 0;
  display: block;
  border-top: 20px solid #187dbc;
  border-bottom: 20px solid #187dbc;
  border-left: 16px solid transparent;
  border-right: 20px solid #187dbc;
  position: absolute;
  opacity: 0.6; 
  right: 0;
  top: 0;
  border-radius: 0 5px 5px 0;  
}

 .container{
   margin:0 auto;
    width:600px; 
    height:600px; 
    text-align: center;

 }
</style>


<body bgcolor="lightblue">
<header>
<h1><%= title %></h1>
</header>
<nav><hr noshade="noshade">
<span ><a title="管理系统登陆" href="/login" id = "ni">login</a></span><hr noshade="noshade">
<span><a title="管理系统注册" href="/reg">register</a></span><hr noshade="noshade">
<% if(locals.islogin){%>
<span><a title="审核系统" href="/judge">审核用户</a></span>
<% } %>
</nav>
<script>
function Change_1(){document.getElementById("ni").innerHTML='New text!'}

 function   a(){
       var a = document.getElementById("password2_2");
       var b =  document.getElementById("password2");
       if(!(a.value===b.value)){
           alert("两次密码不一样,不允许注册，注册按钮消失,填写正确后会消失");
            document.getElementById("reg").style.visibility = 'hidden';
       }
       else
        document.getElementById("reg").style.visibility='visible';
    }
    function b(){
       var b =  document.getElementById("password2");
       if(b.value.length)
       if(b.value.length < 6){
          alert("密码长度有问题,不允许注册，注册按钮消失,填写正确后会消失");
            document.getElementById("reg").style.visibility = 'hidden';
       }
       else
        document.getElementById("reg").style.visibility='visible';
    }

    
    </script>
<article>